{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="fw-normal">{{ matiere.id }}</span></td>
            </tr>
            <tr>
                <th>Nom</th>
                <td><span class="fw-normal">{{ matiere.nom }}</span></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_matiere_index') }}">back to list</a>

    <a href="{{ path('app_matiere_edit', {'id': matiere.id}) }}"><button class="btn btn-danger me-2">Modifier</button></a>

    {{ include('matiere/_delete_form.html.twig') }}
{% endblock %}
