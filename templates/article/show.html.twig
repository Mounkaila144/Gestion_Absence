{% extends 'base.html.twig' %}

{% block title %}article{% endblock %}

{% block body %}
    <h1>article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="fw-normal">{{ article.id }}</span></td>
            </tr>
            <tr>
                <th>Nom</th>
                <td><span class="fw-normal">{{ article.nom }}</span></td>
            </tr>

            <tr>
                <th>Saison</th>
                <td><span class="fw-normal">{{ article.saison }}</span></td>
            </tr>
            <tr>
                <th>Episode</th>
                <td><span class="fw-normal">{{ article.episode }}</span></td>
            </tr>
        </tbody>
    </table>
<a href="{{ path('admin_article_index') }}"><button type="submit" class="btn btn-primary me-2">Moifier</button></a>
    Cancel
    <a href="{{ path('admin_article_index') }}"><button class="btn btn-danger">retourt</button></a>
    {{ include('admin/article/_delete_form.html.twig') }}
{% endblock %}
