{% extends 'base.html.twig' %}

{% block title %}Filier{% endblock %}

{% block body %}
    <h1>Filier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="fw-normal">{{ filier.id }}</span></td>
            </tr>
            <tr>
                <th>Nom</th>
                <td><span class="fw-normal">{{ filier.nom }}</span></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_filier_index') }}">back to list</a>

    <a href="{{ path('app_filier_edit', {'id': filier.id}) }}"><button class="btn btn-danger me-2">Modifier</button></a>

    {{ include('filier/_delete_form.html.twig') }}
{% endblock %}
